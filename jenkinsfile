pipeline {
   agent {docker 'maven:3.5-alpine'}

   stages {
      stage('Checkout') {
         steps {
            git 'https://github.com/Ankush-dhyani/spring-petclinic.git'
         }
      }
      stage('build'){
          steps {
              sh 'mv clean package'
          }
      }
   }
}
